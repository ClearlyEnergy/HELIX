<?xml version="1.0"?>
<document>
	{% for property in content %}
		<Property> 
			<Location>
				<Address>
					<StreetName>
						{{property.property.address_line_1}}
					</StreetName>
					<StreetNumber>
					</StreetNumber>
					<City>
						{{ property.property.city }}
					</City>
					<StateOrProvince>
						{{ property.property.state }}
					</StateOrProvince>
					<PostalCode>
						{{ property.property.postal_code }}
					</PostalCode>
					<Country>
						USA
					</Country>
				</Address>
				{% if property.gis %}
				<GIS>
					{% for k,v in property.gis.items %}
	                <{{ k }}>{{ v }}</{{ k }}>
					{% endfor %}
				</GIS>
				{% endif %}
			</Location>	
			{% if property.assessments %}
			<Structure>
				<Performance>
					<GreenVerification>
					        {% for assessment_property in property.assessments %}
					        <GreenBuildingVerification>
					                {% for k,v in assessment_property.to_reso_dict.items %}
					                <{{ k }}>{{ v }}</{{ k }}>
					                {% endfor %}
					        </GreenBuildingVerification>
					        {% endfor %}
					</GreenVerification>
				</Performance>
			</Structure>
			{% endif %}
			{%  if property.measurements %}
			<Utilities>
				{% for k,v in property.measurements.items %}
		        <{{ k }}>{{ v }}</{{ k }}>
				{% endfor %}
			</Utilities>
			{% endif %}
		</Property>
	{% endfor %}
</document>
