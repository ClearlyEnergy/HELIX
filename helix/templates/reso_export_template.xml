<?xml version="1.0"?>
<document>
	{% for property in content %} 
		<Location>
			<Address>
				<StreetName>
					{{property.property.normalized_address}}
				</StreetName>
				<StreetNumber>
				</StreetNumber>
				<UnitNumber>
					{{ property.property.address2 }}
				</UnitNumber>
				<city>
					{{ property.property.city }}
				</city>
				<PostalCode>
					{{ property.property.postal_code }}
				</PostalCode>
				<Country>
					USA
				</Country>
			</Address>
		</Location>	
		<Structure>
			<Performance>
				<GreenVerification start="{{ start_date}} " end="{{ end_date }}">
				        {% for assessment_property in property.assessments %}
				        <GreenAssessmentProperty date="{{ assessment_property.date }}">
				                {% for k,v in assessment_property.to_reso_dict.items %}
				                <{{ k }}>{{ v }}</{{ k }}>
				                {% endfor %}
				        </GreenAssessmentProperty>
				        {% endfor %}
				</GreenVerification>
			</Performance>
		</Structure>
		{%  if property.measurements %}
		<Utilities>
			{% for k,v in property.measurements.items %}
	        <{{ k }}>{{ v }}</{{ k }}>
			{% endfor %}
		</Utilities>
		{% endif %}
	{% endfor %}
</document>
